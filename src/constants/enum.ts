export enum ERole {
    USER = 1,
    ADMIN = 2,
}

export enum EUserStatus {
    INACTIVE = 1,
    ACTIVE = 2,
    DELETED = 3,
    BANNED = 4,
}

export enum ETypeDiscount {
    PERCENTAGE = 1,
    FIXED_AMOUNT = 2,
    ORDER_TOTAL = 3,
}
